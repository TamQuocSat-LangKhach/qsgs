local prefix = "packages.qsgs.pkg.qwisdom.skills."

return {
  require(prefix .. "juao"),
  require(prefix .. "tanlan"),
  require(prefix .. "shicai"),

  require(prefix .. "yicai"),
  require(prefix .. "beifa"),

  require(prefix .. "houyuan"),
  require(prefix .. "chouliang"),

  require(prefix .. "bawang"),
  require(prefix .. "weidai"),

  require(prefix .. "longluo"),
  require(prefix .. "fuzuo"),
  require(prefix .. "jincui"),

  require(prefix .. "badao"),
  require(prefix .. "wenjiu"),

  require(prefix .. "shipo"),
  require(prefix .. "gushou"),
  require(prefix .. "yuwen"),

  require(prefix .. "shouye"),
  require(prefix .. "jiehuo"),
  require(prefix .. "shien"),
}
